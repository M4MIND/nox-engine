import { Camera } from './../camera/Camera';
import { Renderer } from './../renderer/Render';
import { CoreBehavior } from './CoreBehavior';

export class Component extends CoreBehavior {
    public onRendererObject(renderer: Renderer, camera: Camera): void {
    }

    public awake(): void {
    }

    public start(): void {
    }

    public update(): void {
    }
}